% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/activity.R
\name{analyze_trip_purpose_distance}
\alias{analyze_trip_purpose_distance}
\title{Analyze mobility flows by trip purpose and distance}
\usage{
analyze_trip_purpose_distance(
  mobility_data,
  distance_bands = c(2, 10, 50, 200),
  min_trips = 10,
  include_temporal = TRUE
)
}
\arguments{
\item{mobility_data}{Data frame with activity and distance columns}

\item{distance_bands}{Vector of distance thresholds (default: c(2, 10, 50, 200))}

\item{min_trips}{Minimum trips threshold (default: 10)}

\item{include_temporal}{Whether to include temporal analysis (default: TRUE)}
}
\value{
List with trip purpose-distance analysis results
}
\description{
Examines the relationship between trip purposes, distances, and mobility patterns
leveraging activity and distance information
}
\examples{
\dontrun{
# Analyze trip purposes by distance
purpose_distance <- analyze_trip_purpose_distance(mobility_data)
print(purpose_distance$purpose_distance_matrix)

# Custom distance bands for urban analysis
urban_purpose <- analyze_trip_purpose_distance(
  mobility_data,
  distance_bands = c(1, 5, 15, 30)
)

# Include temporal patterns
temporal_purpose <- analyze_trip_purpose_distance(
  mobility_data,
  include_temporal = TRUE
)
}
}
